// This is a placeholder file that previously contained sound functionality
// We're keeping it to maintain compatibility with any imports, but removing the actual sound functionality

export const playSound = (soundName: string) => {
  // No-op function to prevent errors
  return
}

export const playSoundIfEnabled = (soundName: string, isEnabled = true) => {
  // No-op function to prevent errors
  return
}

export const preloadSounds = () => {
  // No-op function to prevent errors
  return
}

export const isSoundEnabled = () => {
  // Always return false since we're disabling sounds
  return false
}

export const toggleSound = (newState?: boolean) => {
  // No-op function to prevent errors
  return false
}
